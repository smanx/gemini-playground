{
  "name": "gemini-playground",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "build:netlify": "echo 'No build step required'",
    "deploy:netlify": "netlify deploy --prod"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "wrangler": "^3.60.3",
    "vitest": "2.1.8"
  }
}
